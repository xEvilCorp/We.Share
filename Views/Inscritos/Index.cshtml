@model IEnumerable<WeShare.Web.Data.InscritosCanal>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Notificar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CanalNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Notificar)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CanalNavigation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserNavigation.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
